class UtilityMethods {

    static handleURLClick = (url) => {
        // Handle the click action, for example, navigate to a new page.
        window.open(url, '_blank');
    };

}

export default UtilityMethods;